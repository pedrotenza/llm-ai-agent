
cd G:\software-projects\llm-applications\llm-ai-agent

venv\Scripts\activate
pip freeze > requirements.txt


python -m src.rag.rag_pipeline

# 1. Check what has changed
git status

# 2. Stage the changes
git add .

# 3. Create a commit
git commit -m "Description of the changes"

# 4. Push the changes to the remote repository
git push



Was ist eine Gefährdungsbeurteilung?




https://chatgpt.com/c/6a54f3cd-66c4-83eb-9bc4-63480baecfe2




                    ┌──────────────┐
                    │    Usuario   │
                    └──────┬───────┘
                           │
                           ▼
                    ┌──────────────┐
                    │     Agent    │
                    │   LLM/Brain  │
                    └──────┬───────┘
                           │
             ┌─────────────┼─────────────┐
             │             │             │
             ▼             ▼             ▼
          RAG Tool      API Tool      Memory
             │             │             │
             ▼             ▼             ▼
          FAISS         REST API     Conversation
             │
             ▼
         Documents




llm-ai-agent/
│
├── documents/
│
├── vector_store/
│
├── requirements.txt
│
└── src/
    │
    ├── rag/
    │   ├── pdf_loader.py
    │   ├── embeddings.py
    │   ├── vector_store.py
    │   └── rag_pipeline.py
    │
    ├── agent/
    │   ├── agent.py
    │   ├── tools.py
    │   ├── memory.py
    │   └── prompts.py
    │
    ├── api/
    │   ├── client.py
    │   └── services.py
    │
    ├── config.py
    └── main.py















# LLM AI Agent with RAG

An LLM-based AI Agent that uses a **RAG pipeline** to retrieve information from internal PDF documents and generate context-aware responses using a local LLM.

The Agent is designed to use RAG as its primary knowledge source and later use **web search as a fallback** when the required information is not available in the internal documentation.

## Architecture

```text
                         ┌──────────────────┐
                         │      User        │
                         └────────┬─────────┘
                                  │
                                  ▼
                         ┌──────────────────┐
                         │     AI Agent     │
                         │                  │
                         │  Reasoning       │
                         │  Tools           │
                         │  Memory          │
                         │  Prompts         │
                         └────────┬─────────┘
                                  │
                 ┌────────────────┼────────────────┐
                 │                │                │
                 ▼                ▼                ▼
          ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
          │   RAG Tool  │  │ Web Search  │  │   Memory    │
          └──────┬──────┘  │    Tool     │  └─────────────┘
                 │         └─────────────┘
                 ▼
          ┌──────────────────┐
          │  FAISS + PDFs    │
          └──────────────────┘
```

**Information priority:**

```text
User → Agent → RAG → sufficient information → Answer
                 │
                 └── insufficient → Web Search → Answer
```

## Project Structure

```text
llm-ai-agent/
│
├──    documents
│
├──    requirements.txt
│
├──    src
       │
       │
       ├── rag/
       │   ├── pdf_loader.py
       │   ├── embeddings.py
       │   ├── vector_store.py
       │   └── rag_pipeline.py
       │
       ├── agent/
       │   ├── agent.py
       │   ├── tools.py
       │   ├── memory.py
       │   └── prompts.py
       │
       ├── main.py

```

## Technologies

* Python
* LangChain
* FAISS
* Sentence Transformers
* `all-MiniLM-L6-v2`
* Ollama
* Qwen 2.5 0.5B

## Installation

```bash
pip install -r requirements.txt
ollama pull qwen2.5:0.5b
```

Place PDF documents in:

```text
documents/
```

Run:

```bash
python main.py
```

## Roadmap

* [x] PDF-based RAG pipeline
* [ ] RAG as an Agent tool
* [ ] Web search fallback
* [ ] Conversation memory
* [ ] Additional tools
* [ ] Source/page references
* [ ] Agent evaluation

## Goal

Build a modular local **LLM AI Agent** that combines:

```text
RAG + Tools + Memory + LLM
```

with internal documentation as the primary knowledge source and web search as a fallback.
